# 评分与问答改进记录

用户于2026-09-06确认先完善产品，再重做PPT和PDF；暂停材料定稿，视频不再推荐为提交附件。此前output/submission是旧候选版本，不能代表本轮新规则。

## 本轮目标

1. 修复负PE获得高分、数据缺失获默认分、个股实时与同业历史混比等错误。
2. 五维原始指标、规则贡献、日期和缺项可直接查看；五维完整才参与等权综合排名。
3. 资金维按同五日成交规模归一化；披露质量与财务安全度分离，评分假设与验证结论分离。
4. 改善财报工具选择、单位和输出一致性；保留旧评测，建立新的开发验证版本。

## 实施边界

- 评分v2是透明研究规则，不是训练完成或经收益验证的专业评级。ROE和增长阈值是明示的研究假设，正PE分档仅是估值观察，不证明周期行业低估。
- 缺项、非正PE不可适用以及源数据日期不合格均如实展示；不为维持原排名覆盖率补默认值。
- 所有比较取同一原子发布的批次；财务期间一致，市场维度按共同实际交易日比较。旧v1快照保留，不能还原其原始输入，故不转换成新规则结果。
- 财报披露日期、来源交易日期和抓取时间分开；联网失败不使整页不可用，但会降低可评价覆盖。
- 原90次任务的输入、首次输出与评分保留。新版本改动不覆盖旧冻结源，重新验证不能宣称独立盲测。

## 状态

- [x] 根因核查与范围确认。
- [x] 纯五维评分引擎v2及边界测试。
- [x] 原始观察采集、单位与日期校验、140家公司批次。
- [x] 同批次雷达、同业排名和明细页面。
- [x] 问答工具原值展示与有限范围一致性检查。
- [x] 新开发题集及真实联网对照，记录改进与残留问题。
- [x] 最终回归与运行服务更新。

本轮不继续制作视频，也不在新产品验证前重做提交包。PPT/PDF将在规则与实际功能稳定后按完整研究流程重编。

## 已确认的回归结果

- v2合成数据仅用于UI回归：14个分组、桌面和手机共28组排名悬停通过，不将合成数据作为实际评级。
- 实际评分页桌面1280宽、手机390宽均能加载数据，切换公司立即清空旧结果，无页面横向溢出。
- 修复研报公司解析中的首项误选，5项解析回归通过。
- 修复RSI全平行情误判100、RSI为0被当缺失，5项技术边界回归通过。
- 成交额换算精度、问答对象/期间误报及参数来源标签修正后，最终完整测试318项通过（45.08秒）。问答评测后修正没有重新计入12题成绩，原9项冻结输入哈希均通过归档校验。
- 利润工作区7家公司、7类模型及手机图表回归通过；主营构成和宏观周期标题问号在桌面/手机均通过。华阳新材实际缺项样本不计算综合分、不显示排名，雷达保留断点且不填充虚构多边形。
- 当前服务为 `http://localhost:7862/`，临时合成排名测试服务已关闭。

## 数据与问答实测

- 已核实搜狐官方历史页“成交金额(万)”及接口列映射，备用成交额按10000转为元。最终批次 `ff4089c87992425a976c03fe9892e7c6` 含140家公司、61家完整、79家部分缺失。完整行市场日期均2026-09-04、财务期间2025-12-31。重试复用同日期已成功来源；相较27家完整的基准无成功维度退化。
- 当前文件与批次归档逐字节一致，140行原始值重新运行评分引擎与保存得分逐行一致。剩余缺项包括57条资金接口503、7条技术接口501、14条负/缺增长基期、1条ROE不符，原因可重叠；未持续无限重试。
- 新问答验证使用12道开发题、3种模式、每模式16轮。固定流程12/12任务通过，自由Agent6/12，直接回答2/12；55次API请求、147088 tokens、0API或执行错误。题目已知、财务标准值来自同一缓存引擎，不能替代独立会计核验或盲测。详细逐题审计见 `output/evaluation_v2/audit.md`。
- 自由Agent在结构化情景题漏调用计算器，仍有算术错误。另做生产默认文本S02烟测，正确调用并输出32900元/吨可售金属，2次API、8070 tokens；单次成功不能证明稳定或把差异归因JSON格式。所有旧结果及新冻结资料保留。

## 后续优先项

1. 用相同问题、工具集合与提示词控制变量，定位JSON输出与工具选择的交互；新实验另建冻结版本，不覆盖本轮。
2. 在数据接口恢复后补齐剩余网络缺项；真实不适用保留。若验证评分有效性，应另设计历史可得数据、时间切分及样本外评估，不能拿当前快照倒推收益。
3. 产品规则及问答限制稳定后，再重编完整项目PPT/PDF。

## 研报技术面接口回退与Wind准备

用户反馈紫金矿业研报技术面失败后，实测 `get_technical_indicator('601899')` 返回腾讯日K HTTP501。本地评分原始行情截至2026-09-04完整，但研报正文工具此前只请求实时接口。

已为该工具添加实时失败/数据不足时的近期本地行情回退：公司、批次、期间、复权与日期校验通过才使用，超过4个自然日或无效行情不回退；输出明确标注本地快照日期和非实时来源。实时返回未复权day不再冒充前复权qfqday。紫金实际验证得到收盘33.35、RSI52.7等指标，无需等待Wind导入。

Wind尚未导入；导出要求见 [Wind数据导出清单](Wind数据导出清单.md)，140家Wind证券代码已生成在 `data/wind_import/证券代码140家.txt`。先统一行情口径，供应商资金流定义和分部经营数据仍需单独核对。

## 工具选择与统一研究输入

- 后续用户确认先做两项：解决计算器漏调用，以及共用本地研究快照，暂不依赖Wind。
- 16次配对决策仅改变API输出格式：text调用7/8、JSON2/8。生产将工具选择与JSON终稿排版分开，保持auto、不加关键词路由和分类调用。8个实际情景运行输入/结果/单位均核对通过，缺参及概念两例不强制工具。详见 `output/tool_choice_v3/findings.md`，不能据此宣称所有问题稳定。
- 一次研报先捕获评分批次、指定财报期间、经营披露和截至截止日的宏观数据。正文、雷达、均值和排名共用捕获批次；利润桥、分部图、证据与敏感性共用财务分析对象。生成过程中外部文件更新不会改变已捕获输入。
- 研报不再额外实时取数，也不以另一期财务或画像均分占比补缺。独立评分查询、利润面板主动加载/换期是新的研究请求，可采用其他批次或期间。
- 输入、证据及同业原始快照归档至 `output/report_runs/<研究编号>/context.json`，最终版本还保存实际模型请求 `request.json` 和正文/usage `report.json`。不含密钥；旧生成样本可能没有后加的请求归档。
- 同期间分部无法勾稽时不画收入占比图，紫金矿业的内部抵销等记录仍在分部比较中保留。钯/铂识别在分部图和敏感性中统一。
- 验证：339项自动测试通过；真实紫金与南山研报成功，南山图文/利润期间2025-12-31、行情2026-09-04在桌面和手机回归通过；主服务已加载新代码。
- 限制：JSON模式多一次无工具终稿调用。结构化无证据金额门禁只覆盖精确contribution字段；模型文字仍可能出现概括不严谨、单位排版或技术术语误用，不宣称全文核验。数据共同捕获也不等于完整历史点时性。
